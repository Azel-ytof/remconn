name: Rust

on: [push, pull_requests]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    steps:
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
